# 依赖
node_modules/

# 运行时产物（本仓库不跑三窗口，这些是自检副作用）
# 注：wf/ 是接入项目里「人要读、要进 git」的产出目录（见 08-dist），
# 但本仓库自己不跑流水线，所以这里只会出现测试副作用。
# 人工验证凭证不放 wf/ —— 它是本仓库的资产，放 docs/verification/。
.pi/
wf/

# 临时文件
*.tmp
.tmp-*

# 人工私域：AI 禁止读取
.human-only/
