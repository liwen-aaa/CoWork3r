@echo off
rem arch.bat - single-window entry for the arch role. Run from the project root.
rem (trio.bat opens all three; use this when you only want one window.)
title ARCH
set WF_ROLE=arch
cd /d "%CD%"
pi
