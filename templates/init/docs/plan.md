# <项目名> 规划书

> 澄清：<日期> ｜ 断言逐条签字：**未**
>
> **这份文件是断言源。** arch 照它分发、dev 照它实现、tester 照它判定、gate 照它校验 —— 四个读者绑同一份。
> 完整语法见上游 `templates/plan.md`（可运行示例）。
>
> 三条硬规则：
> 1. 已 PASS 的里程碑节**冻结**（回头改 = 伪造记录）
> 2. 断言节**只有人能改**（arch 可改状态标记 / 未决表 / 风险节）
> 3. **能塌缩**：最小合法形态 = 一个里程碑 + 一条断言

## 目标

<一句话：要什么>
成功 = <一句话：怎么算做成了>
不做：<明确排除的，防止实现方过度发挥>

<!-- 这三句在 day 0 就写得出来，不需要任何技术决策。写不出「成功 = 什么」，
     说明还没到开工，继续澄清（能不能精确陈述，不是能不能回答）。 -->

## 里程碑 M0 <把这个项目变成可验收的>

<!-- M0 是自举里程碑：它的产出物之一就是「以后怎么验收」这件事本身。
     tester 要跑 `cfg.test` 才能报 PASS，而那条命令正是 M0 的产物 ——
     所以 M0 期间 wf.config.json 的 test 是 null（显式降级，不是静默缺失），
     断言只能走路径存在性 [auto] 与 [human]。

     M0 的收尾动作 = 把 test 从 null 改成真命令。从那一刻起 gate 才有牙。 -->

### 断言

- [auto] 存在 <某个能证明骨架跑起来了的路径>，且 `<某条能跑的命令>` 退出码为 0
- [human] <只有你能判的那件事，用你自己的话> —— <为什么只有人能判>

### 涉及

- <预估路径或目录>

## 未决

<!-- 已能精确陈述、但还没定的决策。三列：是什么 —— 标记 + 归谁 —— 前置。
     [auto] 的可以 /research 派出去查；[human] 的等人。
     一条都没有就写「无」——那是合法的最小形态，不是没想。 -->

- 无
