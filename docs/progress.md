# 进度（生成物，勿手改）

> 由 `npm run docs:progress` 生成。**权威是 [`plan.md`](plan.md) 的里程碑标题**（`✅` 标记）
> + `docs/modules/` 剩余份数 + vitest 实测计数。
>
> 进度曾同时手写在 README、modules 散文、modules 表三处，全部过时且互不一致——
> 那是 D-04 + D-02 的合并症状。手写数字同理：三处写过 19 / 23 / 78，没一处对得上实测。

| 里程碑 | 内容 | 断言 | 用例 | 状态 |
|---|---|---|---|---|
| M1 | 通道层可用 | 5 auto + 1 human | 24/24 绿（11 文件） | ✅ 已验收 |
| M2 | 协议表驱动路由 | 5 auto | 48/48 绿（4 文件） | ✅ 已验收 |
| M3 | 配置与规约 | 8 auto + 1 human | 61/61 绿（11 文件） | ✅ 已验收 |
| M4 | 规划书解析 | 4 auto + 1 human | 63/63 绿（11 文件） | ✅ 已验收 |
| M5 | 拦截链 | 2 auto + 1 human | 72/72 绿（10 文件） | ✅ 已验收 |
| M6 | 三窗口跑通 | 4 auto + 2 human | 70/70 绿（19 文件） | ✅ 已验收 |

已验收 6/6 个里程碑。

## 文档收缩（D-06）

八份模块文档已拆 **8** 份，剩 **0** 份：


代码已落地而文档未收缩 = 两份权威。一个里程碑的第三个 commit 就是拆它对应的那份。

## 常驻机制

离开每轮读序的纪律条目（D-48），它们由 `npm test` 的 pretest 自动跑：

| 条目 | 机制 |
|---|---|
| D-47 只增不改有机制 | `npm run check:disciplines` |
| D-41 自检不得超过运行时 | `npm run check:testsize` |
| D-49 导出必须有生产调用点 | `npm run check:wiring` |
| D-52 配置字段必须有消费点 | `npm run check:config` |

