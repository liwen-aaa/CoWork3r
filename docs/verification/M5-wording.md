# M5 拦截文案清单（生成物，勿手改）

> 由 `npm run wording:gates` 生成。**每条都是实跑输出**，不是从设计文档抄的预期值。
>
> 用途：M5 那条 `[human]` 断言的载体 ——
> 「拦截提示读起来知道下一步该干什么」。判据是 dev 4/4 与 tester 0/4 那组实测：
> 老仓库两边的规约都写了要求、都非空，唯一差别是 dev 的拦截文案里明文列了小节名。
>
> 机器能查的只有形状（reason 非空、含断言编号，T3 在查）。
> 「读完知不知道该干什么」没有机械判据，所以这条留人工（D-44：挂在产出它的里程碑）。

共 20 条。

## G_plan

**1. [auto] 断言没有命令也没有路径**

```
里程碑 M1 还不能分发：M1.1 标了 [auto] 但没有可运行的命令、也没有可检查存在性的路径。写不出命令说明它其实是 [human]。位置：M1.1（第 20 行）
```

**2. 里程碑已验收（打过 ✅）**

```
里程碑 M1 还不能分发：里程碑 M1 已验收，节已冻结（D-14）
```

**3. 一条断言都没有**

```
里程碑 M1 还不能分发：里程碑 M1 没有断言
```

## G_artifact_dev

**4. 两条断言一条都没提**

```
a.md 缺这几条断言的结论：M1.1、M1.2。每条断言一行，写上编号（如 M1.1 …）。不要求任何固定小节
```

**5. 漏了其中一条**

```
b.md 缺这几条断言的结论：M1.2。每条断言一行，写上编号（如 M1.2 …）。不要求任何固定小节
```

**6. 产出文件根本不存在**

```
读不到产出文件 nope.md（应在项目根下的相对路径）
```

**7. 产出文件是空的**

```
产出文件 c.md 是空的。要求：M1 每条断言一行结论
```

## G_artifact_report

**8. 断言都覆盖了，但缺判定行**

```
报告 d.md 缺判定行（写「判定：PASS」或「判定：FAIL」）。这是报告与产出说明的唯一结构差别
```

## G_human

**9. questions 是空的**

```
M1 有 1 条 [human] 断言要人判，questions 不能是空的：M1.2
```

**10. 只给了通用方向（老仓库那三条）**

```
questions 没覆盖这几条 [human] 断言：M1.2。每条一个问题，带上编号——通用方向（「结构 / 内容实质 / 引用真实性」这类）不算：老仓库给的就是那三条，四个里程碑没有一个缺陷是人工关卡抓到的
```

## G_source

**11. 自上次投递一个字节没改**

```
x.ts 自上次投递以来没有变化（1 个文件的内容都没动）。产出说明写得再好也不算改动——先改代码
```

**12. 配置里的 source 指向不存在的路径**

```
配置里的 source 指向不存在的路径：no-such（改 wf.config.json 的 source 字段）
```

## G_command

**13. 测试失败（退出码非 0）**

```
测试失败（退出码 1）：node -e "console.log('2 failed | 8 passed'); process.exit(1)"
2 failed | 8 passed
```

**14. 测试超时**

```
测试超时（300ms 未结束）：node -e "setTimeout(()=>{},9000)"
要么它真的慢（调大 wf.config.json 的 testTimeoutMs），要么它卡住了

```

**15. 退出码 0 但输出里没有通过标记**

```
测试退出码是 0，但输出里找不到通过标记 /(passed|PASS)/：node -e "console.log('done')"
done
```

**16. 命令根本不存在（环境问题，D-32）**

```
测试失败（退出码 1）：wf-no-such-command
'wf-no-such-command' 不是内部或外部命令，也不是可运行的程序
或批处理文件。
```

**17. 冷启动自检失败**

```
冷启动自检失败（退出码 2）：node -e "console.error('build error'); process.exit(2)"
build error
```

## G_config

**18. 配置有 fatal，tester 要报 PASS**

```
配置有 1 处 fatal：字段 testPass 不是合法正则：([unclosed
配置坏了不阻止开发，但不能宣布通过——先修 wf.config.json
```

**19. 同一个坏配置下，dev 要投递**

```
（放行）
```

## （启动简报）

**20. test: null —— 人主动声明无法自动测**

```
自动验证已关闭（test: null）：PASS 只靠结构检查 + 人工关卡
```

