# M6 人工验证凭证

> 方式：真 pi 进程验证 ｜ 执行者：人 ｜ 日期：2026-08-21
> 判定：（由人填写）

## 断言一：M6.5 dev 窗口的 `send_task` 工具面里看不到 `arch` 投递目标

### 断言原文

> [human] dev 窗口的 `send_task` 工具面里看不到 `arch` 这个投递目标（**M2 移入**）——
> P2 验的是 schema 对象的内容，而 LLM 看到的是 pi 序列化后的工具描述，中间可能丢字段、
> 变形、截断。schema 按角色生成是「越权在类型层不可能」的全部依据

### 为什么这条必须留人工

P2 测 schema **对象**（typesFrom 隔离），A9 测 wire **接线**（工具面 = sendTaskSchema(role)）。
两者都测不到 pi 序列化那一层——工具注册后到 LLM 上下文之间可能丢字段、变形、截断。
只有真进程能看到「模型实际收到什么」。

### 执行

2026-08-21，真 pi 进程（`pi -e ./extensions/<role>.ts --print`），各窗口让 LLM 自报工具面：

```
WF_ROLE=dev  →  send_task 描述 = 「开发完成，请求验收（投递给 tester）」
                参数 properties = { milestone, body, round, refs }，无 type 枚举
                （dev 唯一能发的 type 是 review_request，schema 按角色省掉 type 参数）

WF_ROLE=arch →  send_task 的 type 枚举 = task_assignment / verification / report
```

（对话原文见当日会话记录，含模型自述工具表。）

### 判据

1. dev 窗口 send_task 的 type 枚举**不含** task_assignment / verification / report
2. dev 窗口 send_task 的 description 里没有「arch」
3. arch 窗口能看到分发类 type——证明隔离是**按角色**的，不是「人人没有」

### 判定

（由人填写，照录签字原文）

---

## 断言二：M6.6 在新建空项目里真开三个窗口，跑通一个里程碑

### 断言原文

> [human] 在一个新建空项目里真开三个窗口，跑通一个里程碑 —— 这是唯一能证明
> 「它真的在运行」的事，mock-pi 永远证明不了

### 为什么这条必须留人工

E1 用同进程 mock-pi 驱动三个适配器，验的是**接线正确**。它不验 pi 的真实行为：
事件时序、`sendUserMessage` 语义、系统提示注入链。mock-pi 永远证明不了「它真的在运行」。

### 前置（已就绪）

- launch 脚本（`cb19801`）：trio.ps1 + 三个 bat，纯 ASCII、防重、wt 三竖排
- 包定义（`0b637d1`）：pi.extensions 指向真实扩展
- wire 接 schema（`e5479f6`）：send_task 工具面按角色生成
- 扩展可被 pi 加载（真进程已验证，见断言一）

### 执行步骤

1. 新建空项目：`mkdir wf-manual && cd wf-manual && npm init -y && mkdir src docs wf`
2. 拷 `templates/plan.minimal.md` → `docs/plan.md`，拷 `templates/wf.config.json` → 根目录，
   改 `test` 为无害命令、`gate` 删掉
3. 三个窗口（或 `launch/trio.ps1 -Root <路径>`）：arch / dev / tester
4. arch 分发 M1（造 src/hello.txt）
5. dev 写 src/hello.txt + wf/dev-output-M1.md（每条断言一行）→ 发 review_request
6. tester 写 wf/test-report-M1.md（判定：PASS + 每条断言一行）→ 发 verdict_pass
7. 人答 [human] 断言 → milestone_passed 回 arch
8. 验证：消息落点、state.json、wf/ 记录、/status 四行

### 判据

1. 每个窗口收到该收到的 type（无「窗口开了但不处理」的静默故障）
2. state.json 里程碑/轮次/失败计数按流程变化
3. 全程真 pi 进程（事件循环、sendUserMessage 语义），不是 mock

### 判定

（由人填写，照录签字原文）
